#!/bin/sh
# omnissiah: refresh the Codex plugin cache after a rebase/commit --amend.
# Non-fatal: codex-sync.js always exits 0 so it can never abort a git operation.
exec node "$(git rev-parse --show-toplevel)/scripts/codex-sync.js" --quiet
